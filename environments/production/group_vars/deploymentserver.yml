# =================================
# environments/production/group_vars/deploymentserver.yml
# =================================
---
# Apps automatically discovered by cluster-aware-splunk-deploy.yml
# No manual git_apps needed - uses repository structure discovery